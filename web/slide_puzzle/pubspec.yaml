name: slide_puzzle

version: 1.0.0

environment:
  sdk: ">=2.0.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic: ^1.3.0
  provider: ^2.0.0
  test: ^1.3.4

flutter:
  uses-material-design: true
  assets:
    - asset/

  fonts:
  - family: Plaster
    fonts:
    - asset: asset/fonts/plaster/Plaster-Regular.ttf

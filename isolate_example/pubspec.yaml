name: isolate_example
description: A Flutter sample to demonstrate isolates
version: 1.0.0+1

environment:
  sdk: ">=2.5.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^4.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic: ^1.9.0


flutter:
  uses-material-design: true

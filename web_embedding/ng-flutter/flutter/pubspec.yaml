name: ng_companion
description: A flutter app with a counter that can be manipulated from JS.
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.3.1
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  web: ^0.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
